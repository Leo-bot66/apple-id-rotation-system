# Copy to .env and replace the placeholder values.
# Generate with:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ROTATOR_FERNET_KEY=replace-with-a-fernet-key
ROTATOR_DB_PATH=./data/rotator.sqlite3
ROTATOR_IMAP_TIMEOUT_SECONDS=20
ROTATOR_IMAP_CA_FILE=
